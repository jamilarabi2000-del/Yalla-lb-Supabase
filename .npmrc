# npm 10.x arborist crashes resolving vitest's optional `jsdom@*` peer
# ("Cannot read properties of null (reading 'edgesOut')"), which is what left
# package-lock.json missing @vitejs/plugin-react and every CI run failing at
# the install step. Pin the resolution mode so `npm install` stays reproducible.
legacy-peer-deps=true
