node_modules/
build/
functions/lib/
# Build output. Vercel and Netlify both build from source, so a committed
# dist/ only creates merge conflicts and bloats diffs.
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
serviceAccount*.json
*-service-account.json
