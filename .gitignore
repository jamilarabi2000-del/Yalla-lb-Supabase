node_modules/
build/
functions/lib/
# Build output. Vercel and Netlify both run `npm run build` themselves
# (netlify.toml: publish = "dist"), so a committed bundle would only ever be
# stale, and a reviewer cannot verify it against the committed source.
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
serviceAccount*.json
*-service-account.json
