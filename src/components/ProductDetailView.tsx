import React,{useMemo,useState} from 'react';
import { ArrowLeft,ShoppingBag,Heart,Truck,Minus,Plus,ShieldCheck } from 'lucide-react';
import { useShop } from '../context/ShopContext';
import { ProductCard } from './ProductCard';
export const ProductDetailView:React.FC=()=>{const shop=useShop() as any;const p=shop.selectedProductDetail as any;const [qty,setQty]=useState(1);if(!p)return <div className="p-8 text-center">Product not found.</div>;const related=useMemo(()=>((shop.products||[]) as any[]).filter(x=>x.id!==p.id&&x.category===p.category).slice(0,4),[shop.products,p]);const add=()=>{if(typeof shop.addToCart==='function')shop.addToCart(p,qty);else if(typeof shop.addToCartItem==='function')shop.addToCartItem({product:p,quantity:qty});shop.showToast?.('Added to cart','success')};return <section className="max-w-7xl mx-auto px-4 sm:px-6 py-8 space-y-12"><button onClick={()=>shop.goBack?.()} className="flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft className="w-4 h-4"/>Back</button><div className="grid lg:grid-cols-2 gap-8"><div className="rounded-3xl bg-white border overflow-hidden aspect-square flex items-center justify-center"><img src={p.image} alt={p.name} className="max-w-full max-h-full object-contain"/></div><div className="space-y-5"><div><p className="text-sm text-[#B89753] font-bold">{p.brand||p.seller||p.artisan}</p><h1 className="text-3xl sm:text-4xl font-black">{p.name}</h1>{p.arabicName&&<p className="text-xl mt-1" dir="rtl">{p.arabicName}</p>}</div><p className="text-slate-600 leading-7">{p.description}</p>{p.craftStory&&<div className="p-4 rounded-2xl bg-slate-50"><b>Story</b><p className="text-sm text-slate-600 mt-1">{p.craftStory}</p></div>}<div className="flex items-end gap-3"><span className="text-3xl font-black">{shop.formatPrice?.(p.priceUSD)||`$${p.priceUSD}`}</span>{p.originalPriceUSD&&<del className="text-slate-400">{shop.formatPrice?.(p.originalPriceUSD)||`$${p.originalPriceUSD}`}</del>}</div><div className="flex items-center gap-2"><span className="text-sm text-slate-500">Stock: {p.stock}</span>{p.stock>0&&<span className="text-xs text-emerald-700 font-bold">Available</span>}</div><div className="flex gap-2"><div className="flex items-center border rounded-xl"><button onClick={()=>setQty(v=>Math.max(1,v-1))} className="p-3"><Minus className="w-4 h-4"/></button><span className="w-10 text-center font-bold">{qty}</span><button onClick={()=>setQty(v=>Math.min(p.stock||1,v+1))} className="p-3"><Plus className="w-4 h-4"/></button></div><button disabled={!p.stock} onClick={add} className="flex-1 py-3 rounded-xl bg-slate-900 text-white font-black disabled:opacity-40 flex items-center justify-center gap-2"><ShoppingBag className="w-5 h-5"/>Add to cart</button><button onClick={()=>shop.toggleFavorite?.(p.id)} className="p-3 rounded-xl border"><Heart className="w-5 h-5"/></button></div><div className="grid sm:grid-cols-3 gap-3 text-xs text-slate-600"><div className="p-3 rounded-xl bg-slate-50"><ShieldCheck className="w-4 h-4 mb-1"/>Secure checkout</div><div className="p-3 rounded-xl bg-slate-50"><Truck className="w-4 h-4 mb-1"/>Tracked delivery</div><div className="p-3 rounded-xl bg-slate-50">SKU: {p.sellerItemCode||p.id}</div></div></div></div>{related.length>0&&<div><h2 className="text-2xl font-black mb-4">Related products</h2><div className="grid grid-cols-2 md:grid-cols-4 gap-4">{related.map(x=><ProductCard key={x.id} product={x}/>)}</div></div>}</section>};
export default ProductDetailView;
