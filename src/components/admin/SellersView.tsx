import React,{useMemo,useState} from 'react';
import {Store,Search} from 'lucide-react';
import {useShop} from '../../context/ShopContext';
export const SellersView:React.FC=()=>{const shop=useShop() as any;const sellers=shop.sellers||[];const products=shop.products||[];const[q,setQ]=useState('');const rows=useMemo(()=>sellers.filter((s:any)=>!q||[s.nameEn,s.nameAr,s.id,s.sellerCode,s.region].filter(Boolean).some(v=>String(v).toLowerCase().includes(q.toLowerCase()))),[sellers,q]);return <section className="space-y-5"><div><h2 className="text-2xl font-black">Sellers</h2><p className="text-sm text-slate-500">Seller identity, ownership and catalog performance.</p></div><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search seller" className="w-full pl-10 pr-4 py-3 rounded-2xl border"/></div><div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4">{rows.map((s:any)=><article key={s.id} className="bg-white border rounded-2xl p-5"><div className="flex items-center gap-3"><div className="w-10 h-10 rounded-xl bg-slate-100 flex items-center justify-center"><Store className="w-5 h-5"/></div><div><h3 className="font-black">{s.nameEn}</h3><p className="text-xs text-slate-500">{s.nameAr}</p></div></div><div className="mt-4 grid grid-cols-2 gap-2 text-xs"><div>Region<br/><b>{s.region||'—'}</b></div><div>Products<br/><b>{products.filter((p:any)=>p.sellerId===s.id).length}</b></div><div>Code<br/><b>{s.sellerCode||s.id}</b></div><div>Status<br/><b>{s.isActive===false?'Inactive':'Active'}</b></div></div></article>)}</div></section>};
export default SellersView;
