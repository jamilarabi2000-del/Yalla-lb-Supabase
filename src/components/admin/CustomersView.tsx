import React,{useMemo,useState} from 'react';
import {Search,Users} from 'lucide-react';
export const CustomersView:React.FC<{dbUsers?:any[]}>=({dbUsers=[]})=>{const[q,setQ]=useState('');const rows=useMemo(()=>dbUsers.filter((u:any)=>{const x=q.toLowerCase();return !x||[u.email,u.name,u.phone,u.uid,u.id].filter(Boolean).some(v=>String(v).toLowerCase().includes(x))}),[dbUsers,q]);return <section className="space-y-5"><div><h2 className="text-2xl font-black">Customers</h2><p className="text-sm text-slate-500">Customer directory backed by Supabase profiles and RLS.</p></div><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search email, name, phone or ID" className="w-full pl-10 pr-4 py-3 rounded-2xl border"/></div><div className="bg-white border rounded-2xl overflow-auto"><table className="w-full text-sm"><thead><tr className="bg-slate-50 text-left"><th className="p-3">Customer</th><th className="p-3">Email</th><th className="p-3">Phone</th><th className="p-3">Role</th></tr></thead><tbody>{rows.map((u:any)=><tr key={u.uid||u.id} className="border-t"><td className="p-3 flex items-center gap-2"><Users className="w-4 h-4 text-slate-400"/>{u.name||'Unnamed'}</td><td className="p-3">{u.email||'—'}</td><td className="p-3">{u.phone||'—'}</td><td className="p-3">{u.role||'customer'}</td></tr>)}</tbody></table></div></section>};
export default CustomersView;
