import React,{useEffect,useMemo,useState}from'react';
import{Plus,Trash2,X,ListOrdered,Save}from'lucide-react';
import{useShop}from'../../context/ShopContext';
import{checkDuplicateProductNumber}from'../../lib/productValidation';
import{normalizeAdminProductPayload,validateAdminProductInput,ADMIN_PRODUCT_DEFAULTS}from'../../lib/adminBehavior';
import{secureRandomInt}from'../../utils/uuid';
import type{Product}from'../../types';

const blank=()=>({name:'',arabicName:'',category:ADMIN_PRODUCT_DEFAULTS.category,artisan:'',seller:'',arabicSeller:'',sellerId:'',origin:ADMIN_PRODUCT_DEFAULTS.origin,priceUSD:15,originalPriceUSD:'',discountPercentage:'',stock:ADMIN_PRODUCT_DEFAULTS.stock,lowStockThreshold:ADMIN_PRODUCT_DEFAULTS.lowStockThreshold,lowStockNotice:'',customStockLabel:'',costPriceUSD:'',image:'',additionalImages:[] as string[],videoUrl:'',videos:[] as string[],weightOrVolume:'',tagsInput:'',keywordsInput:'',arabicKeywordsInput:'',sellerItemCode:'SIC-'+secureRandomInt(100000,1000000),description:'',craftStory:'',seoTitle:'',seoArabicTitle:'',seoDescription:'',seoArabicDescription:'',isNewArrival:true,isFeatured:false,isBestseller:false,isPublished:false,displayOrder:''});

export default function AdminProductManager(){
 const s=useShop()as any,products:Product[]=s.products??[],sellers=s.sellers??[],categories=s.categories??[],toast=s.showToast??(()=>{});
 const add=s.addProduct??(async()=>{}),update=s.updateProduct??(async()=>{}),remove=s.deleteProduct??(async()=>{}),removeMany=s.deleteMultipleProducts??(async()=>{}),reorder=s.reorderProducts??(async()=>{});
 const[form,setForm]=useState<any>(blank()),[editing,setEditing]=useState<Product|null>(null),[modal,setModal]=useState(false),[saving,setSaving]=useState(false),[q,setQ]=useState(''),[seller,setSeller]=useState('all'),[category,setCategory]=useState('all'),[state,setState]=useState('all'),[selected,setSelected]=useState<Set<string>>(new Set()),[ordering,setOrdering]=useState(false),[ordered,setOrdered]=useState<Product[]>([]),[dirty,setDirty]=useState(false);
 useEffect(()=>setOrdered([...products].sort((a,b)=>(a.displayOrder??999999)-(b.displayOrder??999999))),[products]);
 const filtered=useMemo(()=>products.filter(p=>{const x=q.toLowerCase().trim();const hit=!x||[p.name,p.arabicName,p.seller,p.artisan,p.sellerItemCode,p.id,p.description,...(p.keywords||[]),...(p.arabicKeywords||[])].filter(Boolean).some(v=>String(v).toLowerCase().includes(x));return hit&&(seller==='all'||p.sellerId===seller||p.seller===seller||p.artisan===seller)&&(category==='all'||p.category===category)&&(state==='all'||(state==='published'?p.isPublished!==false:p.isPublished===false));}),[products,q,seller,category,state]);
 const set=(k:string,v:any)=>setForm((x:any)=>({...x,[k]:v}));
 const openNew=()=>{setForm(blank());setEditing(null);setModal(true)};
 const openEdit=(p:Product)=>{setEditing(p);setForm({...blank(),...p,tagsInput:(p.tags||[]).join(', '),keywordsInput:(p.keywords||[]).join(', '),arabicKeywordsInput:(p.arabicKeywords||[]).join(', '),additionalImages:p.additionalImages||[],videos:p.videos||p.additionalVideos||[]});setModal(true)};
 const save=async(published:boolean)=>{const v=validateAdminProductInput(form);if(!v.valid){toast(v.message,'warning');return}if(form.sellerItemCode){const d=checkDuplicateProductNumber(form.sellerItemCode,editing?.id||null,products,form.sellerId||undefined,form.seller||form.artisan);if(d.isDuplicate){toast(`Duplicate seller item code: "${form.sellerItemCode}" is already in use by "${d.conflictingProduct?.name}".`,'error');return}}setSaving(true);try{const payload=normalizeAdminProductPayload({...form,tags:String(form.tagsInput||'').split(',').map((x:string)=>x.trim()).filter(Boolean),keywords:String(form.keywordsInput||'').split(',').map((x:string)=>x.trim()).filter(Boolean),arabicKeywords:String(form.arabicKeywordsInput||'').split(',').map((x:string)=>x.trim()).filter(Boolean)},published);if(editing)await update(editing.id,payload);else await add(payload);toast(editing?(published?`Product "${form.name}" updated & published to Public Store!`:`Product "${form.name}" updated & saved as Draft.`):(published?`Product "${form.name}" published live to Public Catalog!`:`Product "${form.name}" saved as Draft (Unpublished).`),'success');setModal(false)}catch(e:any){toast(e?.message||'Unable to save product.','error')}finally{setSaving(false)}};
 const bulk=async(published:boolean)=>{await Promise.allSettled([...selected].map(id=>update(id,{isPublished:published})));setSelected(new Set());toast(`${selected.size} product(s) updated.`,'success')};
 const bulkDelete=async()=>{if(!selected.size||!window.confirm(`Are you sure you want to delete ${selected.size} selected products? This cannot be undone.`))return;await removeMany([...selected]);setSelected(new Set())};
 const Field=({label,k,type='text',optional=false}:{label:string;k:string;type?:string;optional?:boolean})=><label className="space-y-1 text-sm"><span className="font-semibold">{label}{optional?' (optional)':''}</span><input type={type} value={form[k]??''} onChange={e=>set(k,e.target.value)} className="w-full border rounded-xl px-3 py-2.5"/></label>;
 const Text=({label,k,optional=false}:{label:string;k:string;optional?:boolean})=><label className="space-y-1 text-sm"><span className="font-semibold">{label}{optional?' (optional)':''}</span><textarea value={form[k]??''} onChange={e=>set(k,e.target.value)} rows={3} className="w-full border rounded-xl px-3 py-2.5"/></label>;
 return <section className="space-y-5"><div className="flex justify-between gap-3 flex-wrap"><div><h2 className="text-2xl font-black">Products & Inventory Catalog</h2><p className="text-sm text-slate-500">Manage products, prices, stock, media, SEO, publishing and seller ownership.</p></div><div className="flex gap-2"><button onClick={()=>setOrdering(!ordering)} className="px-3 py-2 rounded-xl bg-slate-100 font-bold"><ListOrdered className="inline w-4 mr-1"/>Product Order</button><button onClick={openNew} className="px-4 py-2.5 rounded-xl bg-blue-600 text-white font-bold"><Plus className="inline w-4 mr-1"/>Add Product</button></div></div>{ordering&&<div className="bg-white border rounded-2xl p-4"><div className="flex justify-between"><b>Product Sequence</b><button disabled={!dirty} onClick={async()=>{await reorder(ordered);setDirty(false);toast(`Saved sequence order for all ${ordered.length} products to database!`,'success')}} className="px-3 py-2 rounded-xl bg-blue-600 text-white font-bold"><Save className="inline w-4 mr-1"/>Save Products Order</button></div>{ordered.map((p,i)=><div key={p.id} className="flex gap-3 py-2 border-t mt-2"><b>#{i+1}</b><span className="flex-1">{p.name}</span><button disabled={!i} onClick={()=>{const n=[...ordered];[n[i-1],n[i]]=[n[i],n[i-1]];setOrdered(n);setDirty(true)}}>↑</button><button disabled={i===ordered.length-1} onClick={()=>{const n=[...ordered];[n[i+1],n[i]]=[n[i],n[i+1]];setOrdered(n);setDirty(true)}}>↓</button></div>)}</div>}<div className="grid lg:grid-cols-4 gap-3"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search Arabic, English, SKU, seller, keywords..." className="lg:col-span-2 border rounded-2xl px-4 py-3"/><select value={seller} onChange={e=>setSeller(e.target.value)} className="border rounded-2xl px-3"><option value="all">All Sellers</option>{sellers.map((x:any)=><option key={x.id} value={x.id}>{x.nameEn||x.name}</option>)}</select><select value={state} onChange={e=>setState(e.target.value)} className="border rounded-2xl px-3"><option value="all">All Publish States</option><option value="published">Published</option><option value="hidden">Hidden / Draft</option></select></div><div className="flex gap-2 flex-wrap"><select value={category} onChange={e=>setCategory(e.target.value)} className="border rounded-xl px-3 py-2"><option value="all">All Categories</option>{categories.map((x:any)=><option key={x.id} value={x.id}>{x.nameEn||x.name}</option>)}</select><button onClick={()=>setSelected(selected.size===filtered.length?new Set():new Set(filtered.map(p=>p.id)))} className="px-3 py-2 rounded-xl bg-slate-100 font-bold">{selected.size===filtered.length&&filtered.length?'Clear Selection':'Select All'}</button>{selected.size>0&&<><button onClick={()=>bulk(true)} className="px-3 py-2 rounded-xl bg-emerald-100 font-bold">Publish Selected</button><button onClick={()=>bulk(false)} className="px-3 py-2 rounded-xl bg-amber-100 font-bold">Hide Selected</button><button onClick={bulkDelete} className="px-3 py-2 rounded-xl bg-rose-100 font-bold">Delete Selected</button></>}</div><div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4">{filtered.map(p=><article key={p.id} className="bg-white border rounded-2xl p-4"><div className="flex gap-3"><input type="checkbox" checked={selected.has(p.id)} onChange={e=>{const n=new Set(selected);e.target.checked?n.add(p.id):n.delete(p.id);setSelected(n)}}/><img src={p.image} alt={p.name} className="w-20 h-20 object-contain rounded-xl bg-slate-50"/><div className="min-w-0"><b className="block truncate">{p.name}</b><span className="text-xs text-slate-500">{p.seller||p.artisan}</span><span className="block text-xs font-mono text-slate-400">{p.sellerItemCode||p.id}</span></div></div><div className="flex justify-between mt-3"><b>${Number(p.priceUSD||0).toFixed(2)}</b><span className="text-xs">Stock: {p.stock}</span></div><div className="flex justify-end gap-2 mt-3"><button onClick={()=>update(p.id,{isPublished:p.isPublished===false})} className="px-2 py-1 rounded-lg bg-slate-100">{p.isPublished===false?'Publish':'Hide'}</button><button onClick={()=>openEdit(p)} className="px-2 py-1 rounded-lg bg-slate-100">Edit</button><button onClick={()=>remove(p.id)} className="p-2 text-rose-600"><Trash2 className="w-4"/></button></div></article>)}</div>{modal&&<div className="fixed inset-0 z-50 bg-black/50 flex items-center justify-center p-4"><div className="bg-white rounded-3xl w-full max-w-5xl max-h-[94vh] overflow-y-auto p-6"><div className="flex justify-between mb-5"><div><h3 className="text-2xl font-black">{editing?'Edit Product':'Add Product'}</h3><p className="text-sm text-slate-500">Only Product Name, Category and Price are required. All other fields are optional.</p></div><button onClick={()=>setModal(false)}><X/></button></div><div className="grid md:grid-cols-2 gap-4"><Field label="Product Name" k="name"/><Field label="Arabic Product Name" k="arabicName" optional/><Field label="Category" k="category"/><Field label="Artisan / Seller" k="seller" optional/><Field label="Arabic Seller" k="arabicSeller" optional/><Field label="Origin" k="origin" optional/><Field label="Seller Item Code" k="sellerItemCode" optional/><Field label="Price (USD)" k="priceUSD" type="number"/><Field label="Original Price (USD)" k="originalPriceUSD" type="number" optional/><Field label="Discount %" k="discountPercentage" type="number" optional/><Field label="Stock" k="stock" type="number" optional/><Field label="Low Stock Threshold" k="lowStockThreshold" type="number" optional/><Field label="Low Stock Notice" k="lowStockNotice" optional/><Field label="Custom Stock Label" k="customStockLabel" optional/><Field label="Cost Price (USD)" k="costPriceUSD" type="number" optional/><Field label="Weight / Volume" k="weightOrVolume" optional/><Field label="Display Order" k="displayOrder" type="number" optional/><Field label="Primary Image URL" k="image" optional/></div><div className="grid md:grid-cols-2 gap-4 mt-4"><Text label="Description" k="description" optional/><Text label="Craft Story" k="craftStory" optional/><Text label="SEO Title" k="seoTitle" optional/><Text label="Arabic SEO Title" k="seoArabicTitle" optional/><Text label="SEO Description" k="seoDescription" optional/><Text label="Arabic SEO Description" k="seoArabicDescription" optional/><Field label="Tags (comma separated)" k="tagsInput" optional/><Field label="Keywords (comma separated)" k="keywordsInput" optional/><Field label="Arabic Keywords (comma separated)" k="arabicKeywordsInput" optional/><Field label="Video URL" k="videoUrl" optional/></div><div className="flex gap-5 mt-4"><label><input type="checkbox" checked={!!form.isNewArrival} onChange={e=>set('isNewArrival',e.target.checked)}/> New Arrival</label><label><input type="checkbox" checked={!!form.isFeatured} onChange={e=>set('isFeatured',e.target.checked)}/> Featured</label><label><input type="checkbox" checked={!!form.isBestseller} onChange={e=>set('isBestseller',e.target.checked)}/> Bestseller</label></div><div className="flex justify-end gap-2 mt-6 border-t pt-4"><button onClick={()=>setModal(false)} className="px-4 py-2 rounded-xl bg-slate-100 font-bold">Cancel</button><button disabled={saving} onClick={()=>save(false)} className="px-4 py-2 rounded-xl bg-slate-700 text-white font-bold">{saving?'Saving…':'Save as Draft'}</button><button disabled={saving} onClick={()=>save(true)} className="px-4 py-2 rounded-xl bg-blue-600 text-white font-bold">{saving?'Saving…':editing?'Save & Publish':'Create Product'}</button></div></div></div>}</section>;
}
