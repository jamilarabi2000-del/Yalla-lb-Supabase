import React,{useMemo,useState} from 'react';
import { Package,Truck,CheckCircle2,Clock,Search } from 'lucide-react';
import { Order } from '../types';
import { SearchableSelect } from './ui/SearchableSelect';
interface OrderHistoryProps{orders:Order[];formatPrice:(price:number)=>string;onNavigateProducts:()=>void;language:string}
export const OrderHistory:React.FC<OrderHistoryProps>=({orders,formatPrice,onNavigateProducts})=>{const[q,setQ]=useState('');const[filter,setFilter]=useState('all');const list=useMemo(()=>orders.filter(o=>(filter==='all'||o.status===filter)&&(!q||o.id.toLowerCase().includes(q.toLowerCase()))),[orders,q,filter]);return <section className="space-y-4"><div className="flex flex-wrap gap-2"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search order ID" className="w-full pl-9 pr-3 py-2.5 rounded-xl border"/></div><SearchableSelect value={filter} onChange={e=>setFilter(e.target.value)} className="px-3 rounded-xl border"><option value="all">All statuses</option><option value="pending">Pending</option><option value="confirmed">Confirmed</option><option value="preparing">Preparing</option><option value="shipped">Shipped</option><option value="delivered">Delivered</option><option value="cancelled">Cancelled</option></SearchableSelect></div>{list.length===0?<div className="p-8 bg-white border rounded-2xl text-center text-slate-500">No orders found.<button onClick={onNavigateProducts} className="block mx-auto mt-3 text-indigo-600 font-bold">Browse products</button></div>:<div className="space-y-3">{list.map(o=><article key={o.id} className="bg-white border rounded-2xl p-4"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-3"><div className="w-10 h-10 rounded-xl bg-slate-100 flex items-center justify-center"><Package className="w-5 h-5"/></div><div><div className="font-bold">Order #{o.id}</div><div className="text-xs text-slate-500">{o.date}</div></div></div><div className="text-right"><div className="font-black">{formatPrice(o.totalUSD)}</div><div className="text-xs text-slate-500 flex items-center gap-1 justify-end"><CheckCircle2 className="w-3 h-3"/>{o.status}</div></div></div><div className="mt-3 grid grid-cols-3 gap-2 text-xs text-slate-600"><div>{o.items?.length||0} item(s)</div><div>{o.shipping?.governorate||'—'}</div><div className="text-right">{o.trackingNumber?<><Truck className="inline w-3 h-3"/> {o.trackingNumber}</>:'No tracking'}</div></div></article>)}</div>}</section>};
