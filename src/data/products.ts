import { Product } from '../types';

/**
 * Production catalogue is owned by Supabase.
 *
 * This legacy export is intentionally empty so bundled/sample products can
 * never be mistaken for real catalogue data or used as a recovery seed.
 */
export const INITIAL_PRODUCTS: Product[] = [];
